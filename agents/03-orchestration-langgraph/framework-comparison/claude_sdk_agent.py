"""Email triage agent — Claude Agent SDK implementation.

Same task as langgraph_agent.py. Uses Anthropic's native agent primitives.
"""

from __future__ import annotations

# from anthropic import Anthropic

# TODO: implement same email triage using Claude's tool_use API directly
# (or via claude-agent-sdk if available)
# No external orchestration framework — just the Anthropic SDK
